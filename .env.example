# Authentication Proxy Configuration
# The URL of your API Gateway (lam-studio-api-gateway)
# This handles the GitHub OAuth flow so we don't expose secrets in this desktop app.
AUTH_PROXY_URL=http://localhost:3000
